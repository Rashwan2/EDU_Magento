<?php
/** @var \Magento\Backend\Block\Template $block */
$ticketId = $block->getRequest()->getParam('id');
$deleteUrl = $block->getUrl('supporttickets/ticket/delete', ['id' => $ticketId, 'confirm' => 1]);
$backUrl = $block->getUrl('supporttickets/ticket/index');
?>
<div class="page-main-actions">
    <div class="page-actions">
        <div class="page-actions-buttons">
            <button type="button" class="action-default scalable back" onclick="setLocation('<?= $block->escapeUrl($backUrl) ?>')">
                <span><?= $block->escapeHtml(__('Back to Tickets')) ?></span>
            </button>
        </div>
    </div>
</div>

<div class="admin__fieldset-wrapper">
    <div class="admin__fieldset-wrapper-content">
        <fieldset class="admin__fieldset">
            <legend class="admin__legend">
                <span><?= $block->escapeHtml(__('Delete Ticket Confirmation')) ?></span>
            </legend>
            
            <div class="message message-warning">
                <p><?= $block->escapeHtml(__('Are you sure you want to delete this ticket? This action cannot be undone.')) ?></p>
            </div>
            
            <div class="actions-toolbar">
                <div class="primary">
                    <a href="<?= $block->escapeUrl($deleteUrl) ?>" class="action-default scalable delete primary">
                        <span><?= $block->escapeHtml(__('Delete Ticket')) ?></span>
                    </a>
                </div>
                <div class="secondary">
                    <a href="<?= $block->escapeUrl($backUrl) ?>" class="action-default scalable back">
                        <span><?= $block->escapeHtml(__('Cancel')) ?></span>
                    </a>
                </div>
            </div>
        </fieldset>
    </div>
</div>

<style>
.actions-toolbar {
    display: flex;
    gap: 15px;
    margin-top: 20px;
}

.actions-toolbar .primary,
.actions-toolbar .secondary {
    display: inline-block;
}

.action.delete.primary {
    background-color: #dc3545;
    color: white;
    padding: 10px 20px;
    text-decoration: none;
    border-radius: 4px;
    display: inline-block;
}

.action.delete.primary:hover {
    background-color: #c82333;
    color: white;
    text-decoration: none;
}

.action.back {
    color: #007bff;
    text-decoration: none;
    padding: 10px 20px;
    border: 1px solid #007bff;
    border-radius: 4px;
    display: inline-block;
}

.action.back:hover {
    background-color: #007bff;
    color: white;
    text-decoration: none;
}
</style>
