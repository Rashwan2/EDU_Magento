<?php
/** @var \Magento\Framework\View\Element\Template $block */
?>

<div class="floating-support-button">
    <a href="<?= $block->escapeUrl($block->getUrl('supporttickets/index/create')) ?>" 
       class="floating-support-link" 
       title="<?= $block->escapeHtml(__('Submit Support Ticket')) ?>"
       data-mage-init='{"tooltip": {"delay": 300}}'>
        <span class="support-icon">🆘</span>
        <span class="support-text"><?= $block->escapeHtml(__('Support')) ?></span>
    </a>
</div>

<style>
.floating-support-button {
    position: fixed;
    bottom: 30px;
    right: 30px;
    z-index: 1000;
}

.floating-support-link {
    display: flex;
    align-items: center;
    gap: 8px;
    background: linear-gradient(135deg, #28a745, #20c997);
    color: white;
    text-decoration: none;
    padding: 15px 20px;
    border-radius: 50px;
    font-weight: 600;
    font-size: 14px;
    box-shadow: 0 4px 12px rgba(40, 167, 69, 0.4);
    transition: all 0.3s ease;
    animation: pulse 2s infinite;
}

.floating-support-link:hover {
    background: linear-gradient(135deg, #20c997, #17a2b8);
    color: white;
    text-decoration: none;
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(40, 167, 69, 0.5);
}

.floating-support-link .support-icon {
    font-size: 18px;
    animation: bounce 1s infinite;
}

.floating-support-link .support-text {
    font-size: 14px;
}

@keyframes pulse {
    0% {
        box-shadow: 0 4px 12px rgba(40, 167, 69, 0.4);
    }
    50% {
        box-shadow: 0 4px 12px rgba(40, 167, 69, 0.6), 0 0 0 10px rgba(40, 167, 69, 0.1);
    }
    100% {
        box-shadow: 0 4px 12px rgba(40, 167, 69, 0.4);
    }
}

@keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
    }
    40% {
        transform: translateY(-3px);
    }
    60% {
        transform: translateY(-2px);
    }
}

/* Responsive design */
@media (max-width: 768px) {
    .floating-support-button {
        bottom: 20px;
        right: 20px;
    }
    
    .floating-support-link {
        padding: 12px 16px;
        font-size: 13px;
    }
    
    .floating-support-link .support-icon {
        font-size: 16px;
    }
    
    .floating-support-link .support-text {
        font-size: 13px;
    }
}

/* Hide on support pages */
.support-tickets-index-index .floating-support-button,
.support-tickets-index-create .floating-support-button,
.support-tickets-index-view .floating-support-button {
    display: none;
}
</style>


